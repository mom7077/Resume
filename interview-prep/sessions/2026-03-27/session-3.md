# Session 记录 · 2026-03-27（Session 3）

## 基本信息
- 模式：A（Java 后端）
- 题目数：5
- 平均分：1.8/5

---

## 问答记录

### 题目一：MCT flowsvr/worker 架构 + 任务抢占 + Redis 锁迁移

**追问链：** 两层职责 → 拉取机制 → 防重复 → MySQL锁问题 → Redis锁迁移原因

**候选人回答摘要：**
- flowsvr 负责任务创建/拉取/状态设置，worker 负责调度执行，HTTP 通信 ✅
- 防重复：分布式锁，worker 抢锁后 flowsvr 把任务改为 pending ✅
- MySQL 行锁问题：说"性能差、CPU 不稳定"，但无法解释链路 ❌
- Redis 锁如何解决：猜测"多线程并发导致 CPU 飙升"，无法说清楚阻塞 vs 非阻塞差异 ❌

**评分：** 2/5

---

### 题目二：MCT 冷热数据分离实现

**追问链：** 冷热边界定义 → 触发机制 → 异步实现方式 → 数据不一致处理

**候选人回答摘要：**
- 冷热按访问频率区分，任务完结变冷 ✅
- 热数据存 Redis，冷数据存 MySQL ✅
- flowsvr 触发，异步执行 ✅
- 异步机制：通过状态值改变触发（模糊，说不清楚） ❌
- 不一致处理：回滚（错误，Redis 无法回滚） ❌

**评分：** 2.5/5

---

### 题目三：AlarmTask ZSET 时间分片

**候选人回答：** 不知道

**评分：** 0/5

---

### 题目四：B+ 树 vs B 树 + 聚簇索引 vs 非聚簇索引

**候选人回答摘要：**
- B+ 树叶子节点有链表结构，查询更高效 ✅（方向对但不完整）
- B 树 vs B+ 树核心区别说不清楚 ❌
- 聚簇索引 vs 非聚簇索引：不知道 ❌

**评分：** 1.5/5

---

### 题目五：MySQL 四种隔离级别 + RR 如何解决幻读

**候选人回答：** 忘了，隐约知道有个"副本"解决幻读

**评分：** 1/5

---

## 本次新发现薄弱点

- **ZSET 时间分片完全不掌握**：AlarmTask 核心设计，简历写了但说不出来，需通读源码
- **MySQL 行锁竞争机制不清楚**：无法解释锁等待队列 → 线程阻塞 → 上下文切换 → CPU 飙升链路
- **Redis 跨 MySQL 数据一致性**：错误回答"回滚"，正确思路是先写 MySQL 再删 Redis，以 MySQL 为准
- **MySQL 基础薄弱**：聚簇索引/非聚簇索引、覆盖索引、四种隔离级别、MVCC 均不掌握

---

## 下次建议重点

1. **必须通读 AlarmTask 源码**：重点看 ZSET 任务分片逻辑，能说出 key 结构、score 含义、ZRANGEBYSCORE 用法
2. **背熟 MySQL 四大隔离级别 + MVCC 原理**（undo log、版本链、ReadView）
3. **背熟聚簇索引 vs 非聚簇索引 + 覆盖索引**
4. **背熟线程池七大参数 + 四种拒绝策略**（下次第一题）
